[project]
name = "python-optimization-flags"
version = "0.1.0"
description = "Performance study: impact of python -O / -OO on ML-style code"
authors = [{ name = "Steven Rygaard", email = "steven.rygaard@mila.quebec" }]
requires-python = ">=3.14"
dependencies = ["numpy", "torch", "mlps-shared"]

[tool.uv.sources]
mlps-shared = { path = "../../shared", editable = true }
